# Local ASR dependencies — only needed for offline speech recognition engines
# (whisper / sensevoice / funasr-nano / funasr-mlt-nano / anime-whisper).
#
# Install the core deps first, then PyTorch (CUDA or CPU), then this file:
#   pip install -r requirements-core.txt
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements-local.txt
#   pip install funasr --no-deps
#
# torch & torchaudio are installed separately (CUDA-version specific), see README.

# ASR engines
faster-whisper>=1.0.0
editdistance-s>=1.0.0
omegaconf>=2.3.0
kaldiio>=2.18.0
torch-complex>=0.4.0
soundfile>=0.12.0
librosa>=0.10.0
jaconv>=0.3.0
jamo>=0.4.1
hydra-core>=1.3.0
six>=1.16.0
sentencepiece>=0.2.0
tiktoken>=0.7.0
transformers>=4.40.0

# Model hubs (download local ASR weights)
modelscope>=1.20.0
huggingface_hub>=0.20.0
