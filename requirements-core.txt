# Core dependencies — minimum needed to run LiveTranslate with cloud APIs only
# (ASR engine "openai-audio"/"assemblyai" + any OpenAI-compatible translation API).
#
# This path does NOT require PyTorch or local ASR model weights.
# Install with:
#   pip install -r requirements-core.txt
#
# If you want local (offline) speech recognition — Whisper, SenseVoice,
# FunASR, Anime-Whisper — also install PyTorch and requirements-local.txt.

numpy>=1.24.0
PyYAML>=6.0
httpx>=0.28.0
openai>=1.0.0
psutil>=5.9.0
websocket-client>=1.8.0
pysbd>=0.3.4

# Audio capture (Windows WASAPI loopback)
PyAudioWPatch>=0.2.12

# Text-to-speech (simultaneous interpretation). edge-tts is free, no API key.
edge-tts>=6.1.0
soundfile>=0.12.0

# UI
PyQt6>=6.5.0
